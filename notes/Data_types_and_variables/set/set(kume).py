# Sets are unordered data types.
set_1 = {"sea", "blue", "school", "home"}
print(len(set_1))  # Prints the number of elements in set_1
for i in set_1:  # Prints the elements, but the order is not guaranteed.
    print(i)

